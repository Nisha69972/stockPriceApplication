version: 0.2

phases:
 build:
  commands:
     - docker build -t nisha26/stocksprice:latest .
     - docker push nisha26/stocksprice:latest
 post_build:
   commands:
     -echo Build completed
artifacts:
  files:
    -'**/*'


